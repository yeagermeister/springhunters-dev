
  <div class="card border border-rounded shadow spring-card flex-grow-1 p-2 m-1">
    <h2>Welcome, {{user.name}}!</h2>
    <table>
      <tr>
        <td>First Name:</td><td>{{user.first_name}}</td>
        <td>Last Name:</td><td>{{user.last_name}}</td>
        <td>Email:</td><td>{{user.email}}</td>
        <td>Zipcode:</td><td>{{user.zipcode}}</td>
      </tr>
    </table>


    <form class="Change Info">
      <div class="form-group">
        <label for="changePassword" class="form-label">New Zipcode:</label>
        <input class="form-input form-control" type="text" id="newPassword" />
      </div>
      <div class="form-group">
        <label for="changePassword" class="form-label">New Password:</label>
        <input class="form-input form-control" type="text" id="newPassword" />
      </div>

      <div class="form-group">
        <button class="btn btn-primary" type="submit">Update</button>
      </div>
    </form>
  </div>
</div>


<script src="./js/profile.js"></script>
